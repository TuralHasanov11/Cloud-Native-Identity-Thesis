apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "webapp-server.fullname" . }}-test-connection"
  labels:
    {{- include "webapp-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "webapp-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
